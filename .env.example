# Backend Server Port
PORT=3000

# MongoDB Connection URI
# Replace with your local or cloud MongoDB URI
MONGODB_URI=mongodb://localhost:27017/atlantax

# Note: MOCK_USER_ID is hardcoded in src/backend/server.js for development.
# In a production environment, a proper authentication system would replace this.
# SESSION_SECRET is not currently used.
